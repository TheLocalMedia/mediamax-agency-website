import './globals.css'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: 'MediaMax - Digital Marketing Agency',
  description: 'Professional digital marketing services including social media management, content creation, and digital advertising. Transform your brand with MediaMax.',
  keywords: 'digital marketing, social media management, content creation, digital advertising, marketing agency',
  authors: [{ name: 'MediaMax Agency' }],
  viewport: 'width=device-width, initial-scale=1',
import './globals.css'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: 'MediaMax - Digital Marketing Agency',
  description: 'Professional digital marketing services including social media management, content creation, and digital advertising. Transform your brand with MediaMax.',
  keywords: 'digital marketing, social media management, content creation, digital advertising, marketing agency',
  authors: [{ name: 'MediaMax Agency' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>body>
    </html>html>
  )
}</html>
const inter = Inter({ subsets: ['latin'] })
import './globals.css'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: 'MediaMax - Digital Marketing Agency',
import './globals.css'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: 'MediaMax - Digital Marketing Agency',
  description: 'Professional digital marketing services including social media management, content creation, and digital advertising. Transform your brand with MediaMax.',
  keywords: 'digital marketing, social media management, content creation, digital advertising, marketing agency',
  authors: [{ name: 'MediaMax Agency' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>body>
    </html>html>
  )
}</html>  description: 'Professional digital marketing services including social media management, content creation, and digital advertising. Transform your brand with MediaMax.',
  keywords: 'digital marketing, social media management, content creation, digital advertising, marketing agency',
  authors: [{ name: 'MediaMax Agency' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>body>
    </html>html>
  )
}</html>const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: 'MediaMax - Digital Marketing Agency',
  description: 'Professional digital marketing services including social media management, content creation, and digital advertising. Transform your brand with MediaMax.',
  keywords: 'digital marketing, social media management, content creation, digital advertising, marketing agency',
  authors: [{ name: 'MediaMax Agency' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>body>
    </html>html>
  )
}</html>
